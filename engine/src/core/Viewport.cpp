#include "../../include/core/Viewport.h"

using namespace PhysicsEngine;

Viewport::Viewport()
{
}

Viewport::Viewport(int x, int y, int width, int height) : mX(x), mY(y), mWidth(width), mHeight(height)
{
}

Viewport::~Viewport()
{
}